# TODO — שדרוגי נגישות-למתחילים ובהירות

רשימת שיפורים לפרקים הבנויים (toolkit, kinematics, projectile, forces),
שמטרתם להפוך את התוכן לברור וקל יותר למתחיל לגמרי בפיזיקה.

## הושלם

- [x] **8. פתיחה עם תרחיש מוכר לפני ההגדרה הפורמלית** — שדה `hook` חדש על
      ה-step הראשון של כל שיעור (21/21), מרונדר מעל ה-body.
- [x] **9. הנוסחה ב-`.formula`** — `formula.vars` חדש, מרונדר כ-legend "מי
      זה מי" מתחת לנוסחה; נוסף לכל בלוק נוסחה רלוונטי (18/19).
- [x] **10. פידבק ממוקד-טעות** — `wrongExplain` חדש על שלבי mcq, מוצג במקום
      ה-`explain` הגנרי כשנבחרה אופציה עם תפיסה מוטעית ספציפית (38/39 mcq).
- [x] **11. אוריינטציה ראשונה בסימולציה** — באנר `.sim-orient` דיסמיס-אבל
      ב-simkit.js, לפי `spec.kind`/`spec.orient`, נשמר ב-localStorage; נוסף
      לכל 5 סוגי הסימולציה.
- [x] **12. הנחת ידע קודמת לא מוצהרת** — שדה `prereq` חדש, callout קבוע על
      ה-step הראשון של השיעור הראשון בכל אחד מ-4 הפרקים.

תיעוד הסכמה עודכן ב-[js/engine.js](js/engine.js), [js/simkit.js](js/simkit.js)
ו-[CLAUDE.md](CLAUDE.md) (כולל קונבנציה מחייבת לפרקים עתידיים).

## Backlog (לא בעבודה עדיין)

- [ ] **1. מקרא צבעים** בסימולציות עם כמה וקטורים — אין הסבר איזה צבע = איזה גודל.
- [ ] **2. עצירה/הרצה-מחדש/צעד-אחר-צעד** לסימולציות עם `animate` (תנועה בזמן).
- [ ] **3. מפת-שלבים בתוך שיעור** — כרגע רק progress bar אחוזי, בלי לדעת
      אילו שלבים קונספט ואילו שאלה.
- [ ] **4. סקירת טעויות** במסך הסיום — ריכוז השאלות שהוחמצו עם ההסבר שלהן.
- [ ] **5. שכבת "הסבר שוב"/מילון מונחים** — tooltip על מונח מודגש שמסביר
      אותו בלי לצאת מהשיעור.
- [ ] **6. שלב "דוגמה פתורה"** — סוג שלב חדש (worked example) בין ההסבר
      לשאלה הראשונה.
- [ ] **7. חזרה מרווחת (spaced review) חוצת-פרקים** — סבב חזרה שמערבב
      שאלות מפרקים קודמים.
- [ ] **13. הקראה בקול (TTS) של שאלות/תוכן** — כפתור "🔊" שמקריא את ה-prompt/
      body של השלב. גישה מומלצת: Web Speech API (`speechSynthesis`) — מובנה
      בדפדפן, ללא עלות, ללא שרת, תואם לרוח הפרויקט (zero-dependency). גרסה
      בסיסית: לנקות HTML לטקסט רגיל ולהקריא. גרסה טובה יותר: `fmt.speech()`
      חדש (מקביל ל-`fmt.math()` הקיים) שמתרגם כל פקודת מתמטיקה למילים
      מדוברות (`\frac{a}{b}` → "a over b", `x_f` → "x sub f") כדי שנוסחאות
      לא יוקראו מילולית. קול AI איכותי יותר (ElevenLabs וכו') אפשרי אבל
      דורש מפתח API ופרוקסי ב-`server/` — קפיצת מורכבות משמעותית, לא
      הכרחית להתחלה.

## הערות

- הקבצים הרלוונטיים העיקריים: `js/engine.js` (רינדור שלבים), `js/fmt.js`
  (עיצוב טקסט/נוסחאות), `js/simkit.js` (בניית סימולציות), `css/style.css`
  (עיצוב), ו-`js/lessons/*.js` (תוכן בפועל).
- ראה [CLAUDE.md](CLAUDE.md) לסכמת ה-step ולסדר הטעינה לפני שמוסיפים שדות
  חדשים לסכמה.
